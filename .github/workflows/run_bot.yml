name: Daily Macro Gate Check

on:
  schedule:
    # 한국 시간(KST) 매일 오전 07:00 실행 (UTC 22:00)
    - cron: '0 22 * * 1-5'
  workflow_dispatch: # 수동 즉시 실행 가능

permissions:
  contents: write  # strategy_state.json 자동 커밋 및 Push 권한

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas numpy requests

      - name: Run Macro Gate Strategy
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python main.py

      - name: Commit and push updated state
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add strategy_state.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto: Update strategy state [$(date +'%Y-%m-%d')]" && git push)
